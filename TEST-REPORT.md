# VoiceLedger 0.6 Beta 自检记录

## 已完成的本地检查

- `app.js` JavaScript 语法检查：通过。
- `cloudflare-worker/worker.js` JavaScript 语法检查：通过。
- Worker 已改为火山引擎 `OCRNormal`，请求使用 HMAC-SHA256 签名；AK/SK 只从 Worker Secret 读取。
- 前端与 Worker 的返回结构保持兼容，0.5 之前的书库 / 记录 / 结算数据结构未修改。

## 解析器回归模拟

用用户标准照片对应的“云 OCR 行结果”模拟了火山接口返回：

1. 返音 1047-1066 -> 00:00:10 -> 过滤
2. 返音 151-160 -> 00:00:10 -> 过滤
3. 腿软！恶毒 2-110 -> 00:11:25 -> 匹配简称“恶毒”
4. 双修 394-414 -> 00:00:16
5. 穿成农家 621-631 -> 00:00:08
6. 恐怖故事 601-629 -> 00:05:09
7. 傻子 251-300 -> 00:05:03
8. 穿书后 101-120 -> 00:07:52
9. 限时沉迷 161-220 -> 00:08:00
10. 不是疯笔 447-473 -> 00:25:23 -> 近似纠正到“不是疯批”

模拟结果：10 条文件行全部形成唯一记录，2 条返音被过滤，8 条正式记录的集数和时长全部保持在自己的行区域，没有跨行串时长。

## 还不能在本地证明的部分

这里没有用户自己的火山引擎 AK/SK，因此无法从本地替用户完成一次真实 `OCRNormal` 云端调用。也就是说，本报告验证的是“签名代码语法 + 返回数据解析链 + 标准样例回归”，**真实图片的识字准确率仍要以用户部署后的第一次实际调用为准**。
