# VoiceLedger 0.8 Beta 自检报告

测试日期：2026-09-21

## 已执行

1. `app.js` 通过 `node --check` JavaScript 语法检查。
2. 对 `app.js` 中所有直接 `#id` 选择器与 `index.html` 做静态核对：未发现缺失 ID。
3. 使用轻量 DOM / LocalStorage 测试环境执行完整 `app.js` 启动流程：可完成初始化与 `renderAll()`，无启动时异常。
4. 使用用户真实 0.6 JSON 备份进行兼容测试：成功读取 6 本书、7 条录音记录、0 条结算记录。
5. 在 `Asia/Shanghai` 时区下测试旧数据迁移：两条凌晨 00:47 左右、原先被 0.6 记为 2026-09-21 的记录，按 06:00 规则自动迁移为 2026-09-20；其他白天记录保持不变。
6. 确认升级迁移只处理“原日期与实际录入自然日一致且没有新版 sourceTimestamp”的旧记录，避免覆盖明显的手动指定日期。
7. 核对 0.8 新增页面所需控件：书库排序、结算双视图与筛选、统计二级页面、年度报告年份、首页明细、右上角设置、版本日志均已接入事件逻辑。
8. 使用 Chromium / Playwright 在 430×932 手机视口、`Asia/Shanghai` 时区进行真实 DOM 启动与交互测试：首页、书库排序、统计二级页、年度报告、结算、右上角设置与版本日志均可正常打开，浏览器 `pageerror` 为 0。
9. OCR 核心调用、Cloudflare Worker 请求、重复检测与保存逻辑未重写，继续沿用 0.7 已验证链路。

## 重点回归建议

在 iPhone 真机上优先验证：

- 升级后凌晨旧数据的“今日 / 本月”是否符合 06:00 工作日规则；
- 书库各种排序的升降序；
- 结算“按书籍 / 按甲方”切换和筛选；
- 统计 → 往期统计 / 年度报告二级页面返回路径；
- 首页四类可点击数据卡的明细；
- 设置齿轮与版本更新日志；
- 拍照 OCR、重复检测、编辑 / 删除记录是否继续保持 0.7 行为。

## 说明

自动化浏览器通过内联方式加载前端并完成交互回归；iPhone 真机仍是最终触摸与 PWA 缓存验收环境。代码语法、DOM 依赖、数据迁移、启动流程和主要页面交互均已完成自动检查。
