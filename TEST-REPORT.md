# VoiceLedger 0.7 Beta 自检记录

## 已完成的代码级检查

- `app.js` 通过 Node.js `--check` 语法检查。
- 使用模拟 DOM / LocalStorage 做启动级 smoke test，通过。
- 0.6 本地数据结构迁移：自动补齐 `trashRecords` / `meta`，旧记录保持原日期。
- 工作日测试：切换时间 06:00 时，凌晨 02:00 正确归到前一工作日。
- 重复检测测试：
  - 同书 + 同集数 + 同时长 => `exact`。
  - 同书 + 同集数 + 不同时长 => `conflict`。
- 年度统计基础计算测试：时长与应收金额计算正常。
- HTML 中直接引用的元素 ID 与 `app.js` 对照，无缺失 ID。
- HTML 无重复 ID。

## 0.7 回归原则

- **不修改 0.6 已经验证成功的火山 OCR Worker 核心。**
- OCR 结果仍然先确认再入账。
- 完全重复记录默认不再写入数据库。
- 删除录音记录先进入最近删除，不直接消失。
- JSON 备份继续包含全部数据；0.7 额外带工作日与目标设置。

## 建议真实设备优先测试

1. 拍一张包含“旧记录 + 新记录”的电脑列表，确认旧记录显示“已记录”且保存后只增加新记录。
2. 拍一条集数相同、时长不同的记录，测试“替换旧记录 / 仍然新增”。
3. 导入一批后点击“撤销上次导入”。
4. 在书籍详情修改一条时长，再删除、恢复。
5. 把工作日切换时间设为 06:00，在凌晨测试“今日”归属。
6. 测试指定年份、12 个月快捷入口、年度报告。
7. 导出 CSV，用 Excel 或 Numbers 打开检查中文、时长和金额。
