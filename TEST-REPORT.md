# VoiceLedger 0.9 Beta 自检报告

测试日期：2026-09-24

## 已执行检查

1. **JavaScript 语法**
   - `app.js`：`node --check` 通过。
   - `cloudflare-worker/worker.js`：`node --check` 通过。

2. **0.8 → 0.9 数据迁移**
   - 使用用户真实的 0.8 JSON（6 本书、7 条 AU 记录）做启动测试。
   - 迁移后书籍 ID 与记录均完整保留。
   - `meta.schemaVersion` 更新为 `0.9`。
   - 在写回迁移结果之前成功创建升级前快照。
   - 已有 06:00 工作日迁移结果保持不变，没有再次错误改写日期。

3. **数据保险箱**
   - 自动生成“升级前快照”通过。
   - 设置页能显示快照，提供导出/恢复入口。
   - JSON 导入前与快照恢复前均有二次保险逻辑。

4. **0.9 主要 UI / 信息架构**
   - 底部 5 个主导航正常。
   - 顶部全局搜索能检索旧项目。
   - 书库来源筛选、置顶按钮、归档能力已接入。
   - 呱呱“导入月账”二级页面可进入，默认月份为当前工作月。
   - 设置页 0.9 更新日志与数据保险箱可正常渲染。
   - 浏览器运行测试未发现 JavaScript runtime error。

5. **AU / 呱呱统计隔离**
   - 注入一条 2026-09 呱呱月账后，本月总成品和总收入会增加。
   - “今日成品”保持 0，不会把整个月账错误塞进导入当天。
   - 统计页包含“按来源”维度。

6. **呱呱前端导入流程**
   - 模拟云端返回一条月账：书名、来源、工作量 0.72h、单价 200、官方金额 144.26、结算范围与结算日期。
   - 前端能生成可编辑确认卡并保存为独立呱呱项目 / 月账。
   - 金额保存为官方 `144.26`，没有用 `0.72 × 200` 重算。
   - 有效时长按 `144.26 ÷ 200` 还原更精确值，同时保留 `0.72h` 原工作量。
   - 相同月账第二次导入会显示“已导入”并跳过，不会产生第二条重复数据。

7. **Worker 呱呱表格解析**
   - 用模拟 OCR 坐标测试“表头 + 独立单元格”形式：3 行全部正确解析。
   - 额外测试 OCR 把整行合成一段文本的情况：2 行均能通过兜底解析。
   - 特别核对 `0.72 / 200.00 / 144.26`、跨期结算范围和结算日期没有串列。

8. **视觉检查**
   - 以 430×932 手机视口实际渲染并检查首页、呱呱导入页、设置页。
   - 新增页面过渡、弹层、按钮按压、列表进入动效，并兼容系统“减少动态效果”。

## 限制 / 第一次实机验证重点

- 自动化测试无法代替真实火山 OCR 对用户这张呱呱账单照片的第一次云端识别。Worker 表格解析已做两种 OCR 结构兜底，但真实识别结果仍建议用用户账单做一次实机回归。
- 本地快照仍属于浏览器网站数据；清除 Safari 网站数据可能同时清掉当前数据与本地快照。因此外部 JSON 备份仍需保留。
- 0.9 的动画是 Web/PWA 动效，不是 UIKit/SwiftUI 原生系统导航动画。
